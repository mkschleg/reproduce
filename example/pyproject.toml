[project]
name = "example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "equinox>=0.13.2",
    "jax>=0.8.2",
    "jaxtyping>=0.3.5",
    "optax>=0.2.6",
    "pyyaml>=6.0.3",
    "reproduce",
    "torch>=2.9.1",
    "torchvision>=0.24.1",
    "tyro>=1.0.3",
]

[tool.uv.sources]
reproduce = { path = "../" }
